import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    container: {
        margin: '20px 0',
    },
    btnSubmit: {
        marginTop: '10px',
    },
    gridForm: {
        marginTop: '20px',
    },
    title: {
        textAlign: 'center',
    },
}));
