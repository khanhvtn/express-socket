const eventControllers = require('./event')
const userControllers = require('./user')
const facilityControllers = require('./facility')
module.exports = {
    eventControllers,
    userControllers,
    facilityControllers
}